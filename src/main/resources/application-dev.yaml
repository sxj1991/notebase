spring:
  datasource:
    url: jdbc:mysql://ip:port/db?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowMultiQueries=true
    username: username
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  elasticsearch:
    rest:
      uris: http://127.0.0.1:9200
